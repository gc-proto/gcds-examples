{
  "name": "gcds-examples-site",
  "private": true,
  "version": "0.1.0",
  "description": "Eleventy site to render templates/ with GC Design System layouts",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
  "dev": "ELEVENTY_ENV=development npx @11ty/eleventy --serve",
  "build": "ELEVENTY_ENV=production npx @11ty/eleventy",
  "serve": "npx @11ty/eleventy --serve",
  "check:bilingual": "node scripts/check-bilingual-counterparts.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
